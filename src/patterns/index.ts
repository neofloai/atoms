/**
 * Page layout recipes composed from Neoflo components.
 *
 * Each pattern wraps an end-to-end page structure (dashboard, settings,
 * auth, etc.) so product teams can drop in a fully branded layout
 * without re-deriving spacing, headers, or navigation.
 */

export {};
