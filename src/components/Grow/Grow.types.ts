/**
 * Props for `Grow` — MUI's `GrowProps`, re-exported unchanged.
 *
 * Identical to the shared transition surface described in
 * `src/components/Fade/Fade.types.ts`, with one difference in the type
 * itself: `timeout` additionally accepts the string `'auto'`, which is
 * also its default. See `Grow.tsx` for why that matters.
 */
export type { GrowProps } from '@mui/material';
