/**
 * Props for `Zoom` — MUI's `ZoomProps`, re-exported unchanged.
 *
 * Identical to the shared transition surface described in
 * `src/components/Fade/Fade.types.ts`. `timeout` defaults to
 * `theme.transitions.duration.enteringScreen` / `leavingScreen` and,
 * unlike `Grow`'s, does not accept `'auto'`.
 */
export type { ZoomProps } from '@mui/material';
